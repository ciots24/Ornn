# macOS
.DS_Store

# Claude Code — machine-specific local settings
.claude/settings.local.json

# Installed skill files (restored from skills-lock.json)
.agents/
.claude/skills/

# Environment variables / secrets — never commit (repo is public)
.env
.env.local
.env.*.local
.env.keys
