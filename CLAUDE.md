# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Status

The project directory is currently empty — no source, build config, or version control yet.
There is nothing to document about commands or architecture at this point.

## For the next session

Once code exists here, replace this file with real guidance covering:

- **Commands** — build, run, lint, and test, including how to run a single test.
- **Architecture** — the big-picture structure that only becomes clear after reading several
  files together (module boundaries, data flow, key abstractions), not a file-by-file listing.
- **Project-specific conventions** — anything non-obvious that a newcomer would otherwise get
  wrong.

Re-running `/init` after the initial scaffolding is in place will regenerate this from the actual
codebase.
